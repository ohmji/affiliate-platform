import { CampaignManager } from '../../../components/admin/CampaignManager';

export const metadata = {
  title: 'Campaigns | Affiliate Platform'
};

export default function CampaignsPage() {
  return <CampaignManager />;
}
